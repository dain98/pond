# Character assets

`fisher_sheet.png` was supplied by the project owner for prototype integration.
Its redistribution and commercial-use license should be confirmed before a
public release.

The source image intentionally remains unchanged. It contains an opaque
checkerboard background, which `FisherSpriteFrames` removes from the selected
walking frames when Pond starts.
